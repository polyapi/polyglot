// Package model is OpenAPI → Specification Input → validate/train (POLY-CLI-6).
//
// Translation, AI descriptions, DTO validation, and upserts are Poly HTTP
// calls. The host does not parse OpenAPI locally.
package model
